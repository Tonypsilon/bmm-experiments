package de.berlinerschachverband.bmm.resultdata.data;

import de.berlinerschachverband.bmm.basedata.data.DivisionData;

public record MatchDayData(Long id, Integer matchDayNumber, DivisionData divisionData) {
}
